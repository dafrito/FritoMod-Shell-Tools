#!/bin/bash
PATH=/bin:/usr/bin:$HOME/bin:${0%/*}

case $1 in
	update|u|up)
		shift 
		fm-update.sh $*
	;;
	search)
		shift 
		fm-search.sh $*
	;;
	toc)
		shift 
		fm-toc.sh $*
	;;
	init)
		shift 
		fm-init.sh $*
	;;
	*)
		fm-bin.sh $*
esac
