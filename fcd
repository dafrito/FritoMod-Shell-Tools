#!/bin/bash
if [ $# -eq 0 ]; then
	pushd . >/dev/null
	while [ ! -e .fm ] && [ `pwd` != '/' ]; do
		cd ..
	done
	if [ ! -e .fm ]; then
		popd >/dev/null
	fi
fi
newpath=`lua ${0%/*}/fm-search.sh $*`
if [ 0 == $? ] && [ $newpath ]; then
	cd $newpath
fi
